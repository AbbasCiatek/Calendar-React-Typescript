import type {ReactNode} from "react";
import type {Event} from "@/types.ts"
type Props = {
    children: ReactNode,
    event: Event,
}
export default function AddEditEventDialog({children,event}:Props) {
    return (
        <></>
    );
}